// postcss.config.js
// PostCSS is a tool that transforms CSS using plugins.
// '@tailwindcss/postcss' is the official Tailwind v4 PostCSS plugin.
// It processes your CSS and generates all Tailwind utility classes.

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
